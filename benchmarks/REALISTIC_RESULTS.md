# Realistic profile benchmark scoreboard

No manual profile results recorded yet.

Run, for example:

```bash
bash scripts/bench/bench-one.sh cache-json
```

The command uses 1,000,000 keys by default and updates this file with the latest result for that profile.
